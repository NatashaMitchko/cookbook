services:
  flask:
      build: 
        context: .
      command: gunicorn -w 4 -b 0.0.0.0 manage:app
      volumes:
        - ./services/web/:/usr/src/
      expose:
        - 8000
      env_file:
        - ./.env.prod